# EduAgent v4 企业级精简与算法优化说明

本次修改目标：把系统从“作品集展示页”改成更像真实业务系统的工作台。

## 入口与登录
- 根路径 `/` 不再展示营销首页，未登录时直接进入 `/login`。
- `/login` 是正常登录页，保留管理员登录和访客体验按钮。
- 更新前端登录缓存版本，部署后旧登录态会自动失效，避免直接进入后台。
- 前端没有测试账号和测试密码。

## 信息架构
侧边栏精简为：
- 工作台
- 客户线索
- AI 咨询
- 申请案卷
- 知识库
- 方案引擎
- 检索评测
- 系统日志

删除了面试展示页、Prompt 管理这类容易显得重复的入口，避免企业系统里功能过多过散。

## 算法模块
新增后端接口：

```txt
POST /tools/profile-fit
```

对应方法：

```txt
ToolsService.assessProfile()
```

这是一个本地可解释评分算法，不消耗访客 AI 额度。评分因素：
- GPA / CGPA：30%
- 专业匹配：22%
- 项目经历：24%
- 语言准备：12%
- 预算风险：12%

输出内容包括：
- overall 综合分
- band 等级
- factors 各维度得分
- tierAdvice 冲刺 / 匹配 / 保底配比
- risks 风险点
- nextActions 下一步动作

这个模块适合在后端岗面试时解释为“业务规则算法 + 可解释评分 + AI 生成前置约束”。

## 页面优化
- 工作台文案改成真实系统口吻，减少 HR、面试官、Portfolio 等词。
- 客户线索页把目标国家、目标专业、渠道、切入点改成下拉选项，减少手填。
- 申请案卷页加入适配评分，再生成文书和材料流程。
- 方案引擎页默认展示“适配评分”，再串联选校、文书、销售和材料。
- 结构化 JSON 默认收起，不再直接占据主界面。
- 页面文案减少“AI 味”，改成更短、更像业务系统的表达。

## 构建验证
已验证：

```bash
cd backend && npm run build
cd frontend && npm run build
```
