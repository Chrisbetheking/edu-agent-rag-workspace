# V6 可用性修复说明

本次修复重点：

1. 方案引擎完整流程改为并行编排，避免多个 LLM 调用串行导致 90 秒超时。
2. 前端请求超时从 90 秒提高到 180 秒；失败时会尝试写入 `/tools/client-error-log`，系统日志能看到失败记录。
3. 方案引擎结果改成折叠式展示：执行链路、评分、选校、申请案卷、销售跟进、材料清单分段折叠。
4. 材料清单不再展示大段 JSON，按必交、条件、补充、命名规范、时间节点分组。
5. 知识库“查看切片”接口修复为 `/documents/:id/chunks`，点击后自动滚动到切片预览。
6. 申请案卷的流程卡片改成清晰阶段：背景确认、选校定位、材料准备、文书制作、网申递交、Offer 跟进。
7. 侧边栏收起按钮移到侧边栏顶部，移动端保留“菜单/收起”入口。
8. 手机端继续优化：工具 tab 横滑、结果折叠、表单单列、侧边栏抽屉。

构建验证：
- backend `npm run build` 通过
- frontend `npm run build` 通过
